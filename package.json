{
  "name": "bitroyalty",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Tidak ada proses build diperlukan'",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
