{
  "name": "bitroyalty",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Build tidak diperlukan' && exit 0"
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
